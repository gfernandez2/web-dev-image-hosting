import React, { FC } from 'react';

// Note: FC is "Functional Component", don't think it's needed but may be helpful
const PhotoLibrary: FC = () => {

    /**
     * TODO: Reimplment in React with Typescript
     */
    return (
        <>
        </>
    );
};

export default PhotoLibrary;