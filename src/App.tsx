import React, { FC } from 'react';

import PhotoLibrary from './views/PhotoLibrary/PhotoLibrary';

const App: FC = () => {
    return (
        <div className="App">
            <PhotoLibrary />
        </div>
    );
};

export default App;